# Copy this file to .env and adjust as needed.
# (docker-compose.yml currently sets these directly; .env is only used if you run `node server.js` locally.)

PORT=3000
ADMIN_USER=admin
ADMIN_PASS=flyhigh123
