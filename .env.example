# Copy this file to .env and update the values before going live.
# These values are used by both local Node runs and Docker.

PORT=3000
NODE_ENV=production
ADMIN_USER=eventadmin
ADMIN_PASS=ChangeMeStrongPassword!
